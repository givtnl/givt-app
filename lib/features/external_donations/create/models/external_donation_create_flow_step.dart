/// Steps in the external-donation create flow (for preview + stepper).
enum ExternalDonationCreateFlowStep {
  organisation,
  donationType,
  oneOffDate,
  lastGiftDate,
  startMonthYear,
  success,
}
