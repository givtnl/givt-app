import 'package:flutter/material.dart';

class StripeWebviewHost extends StatelessWidget {
  const StripeWebviewHost({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      body: Center(child: Text('Load Stripe here')),
    );
  }
}
